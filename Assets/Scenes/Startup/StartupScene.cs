using UnityEngine;

/// <summary>
/// Simple startup scene program holder for starting the ProtocolFramework. By itself it does nothing and the scene should be empty;
/// the framework will launch the proper scene when the application loads. This is just a placeholder to start the framework.
/// </summary>

public class StartupScene : MonoBehaviour
{
	void Start()
	{
	}

	void Update()
	{
	}
}
